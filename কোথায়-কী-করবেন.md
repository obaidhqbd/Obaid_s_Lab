# কোথায় কী করতে হবে

## ১) GitHub repository
এই পুরো project-এর সব ফাইল repository root-এ রাখুন। এরপর `main` branch-এ push করুন।

## ২) নিজের ছবি / logo
আপনার photo: `site/assets/portrait.webp` নামেই রাখলে বর্তমান UI-তে সরাসরি কাজ করবে। চাইলে একই জায়গায় আপনার logo, icon ইত্যাদিও রাখতে পারেন।

## ৩) নতুন Project
`content/projects/PROJECT-SLUG/` নামে একটি নতুন folder বানান।
তার ভিতরে `content.md` দিন।
ছবি/video থাকলে একই folder-এ রাখুন।

`content.md`-এর metadata না দিলেও build fallback ব্যবহার করবে। তবে metadata দিলে SEO ও sorting আরও নির্ভুল হবে।

## ৪) নতুন Blog
`content/blogs/BLOG-SLUG/` নামে folder বানান।
তার ভিতরে `content.md` এবং প্রয়োজনীয় image/video রাখুন।

## ৫) Push
শুধু GitHub-এ push করলেই Actions:
build → validation → content indexing → detail page generation → sitemap/robots/feed → GitHub Pages deployment করবে।

## ৬) GitHub Pages
Repository → Settings → Pages → Source হিসেবে GitHub Actions নির্বাচন করুন।
Workflow সফল হলে Pages URL-এ site প্রকাশ হবে।

## ৭) AI Assistant
OpenRouter key কখনো `site/config.js`-এ দেবেন না।
`api/chat.js` আলাদা serverless endpoint হিসেবে Vercel-এ deploy করুন।
Vercel Environment Variables-এ:
`OPENROUTER_API_KEY` যোগ করুন।
চাইলে `OPENROUTER_MODEL`-ও দিন।

তারপর `site/config.js`-এ:
`aiEndpoint: "https://YOUR-DOMAIN/api/chat"`

দিয়ে আবার GitHub-এ push করুন।

## ৮) ফোন নম্বর
পরে ফোন যোগ করলে `site/config.js`-এর `phone` field-এ নম্বর দিন। খালি রাখলে phone button দেখা যাবে না।

## ৯) Local test
Node.js 20+ দিয়ে:
`npm install`
`npm run build`
`npm run validate`

`dist/` folder-ই final generated website।

## ১০) গুরুত্বপূর্ণ
GitHub Pages শুধু static hosting। OpenRouter secret browser-side রাখা নিরাপদ নয়, তাই AI runtime-এর জন্য আলাদা server-side endpoint রাখা হয়েছে।

Lighthouse-এ hosting-dependent বিষয় বাদ দিলে এই architecture performance/accessibility-এর জন্য lightweight রাখা হয়েছে; 100/100-এর hard guarantee বাস্তবসম্মত নয়, কারণ final score environment ও network-এর উপরও নির্ভর করে।
